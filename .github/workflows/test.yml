name: Test

on:
  pull_request:
  workflow_dispatch:
    # Allow manually running the action, e.g. if disabled after some quietness in the source

jobs:
  unit:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Perl versions shipped by the distros we build .deb packages for
        # (see the release workflow's deb matrix):
        #   5.26 Ubuntu 18.04   5.28 Debian 10     5.30 Ubuntu 20.04
        #   5.32 Debian 11      5.34 Ubuntu 22.04  5.36 Debian 12
        #   5.38 Ubuntu 24.04   5.40 Debian 13 / Ubuntu 26.04
        perl:
        - "5.40"
        - "5.38"
        - "5.36"
        - "5.34"
        - "5.32"
        - "5.30"
        - "5.28"
        - "5.26"
    name: Perl ${{ matrix.perl }}
    steps:
    - uses: actions/checkout@v4
    - name: Setup perl
      uses: shogo82148/actions-setup-perl@v1
      with:
        perl-version: ${{ matrix.perl }}
    - run: ./bootstrap.sh
    - run: ./configure --prefix="$HOME/znapzend"
    - run: make test-deps
    - run: ./test.sh
    - run: make install
    - run: "echo \"repo_token: ${{ secrets.GITHUB_TOKEN }}\" > .coveralls.yml"
    - run: perl -I./thirdparty/lib/perl5 thirdparty/bin/cover -report coveralls

  docker:
    runs-on: ubuntu-latest
    name: Docker image builds
    steps:
    - uses: actions/checkout@v4
    # Build the runtime stage too, not just the test stage: the runtime image
    # pulls its own Alpine packages, so this is what catches breakage there
    # (e.g. an Alpine perl version bump) before it reaches a release.
    - name: Build runtime image
      run: docker build --target runtime .
    - name: Run tests within image
      run: docker build --target test .

  changes-file:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    name: CHANGES updated
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Ensure CHANGES was updated
      run: |
        git fetch origin "${{ github.event.pull_request.base.ref }}" --depth=1
        if git diff --quiet "origin/${{ github.event.pull_request.base.ref }}" -- CHANGES; then
          echo "::error::This PR does not update CHANGES. Add a bullet point describing your change under the topmost UNRELEASED entry (see the instructions at the top of the CHANGES file)."
          exit 1
        fi
